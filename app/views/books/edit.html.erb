<h3>Editing Book</h3>

<%= form_with model: @book do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %><br>
  <%= f.submit "Update Book" %>
<% end %>

<p>
  <%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back", books_path %>
</p>